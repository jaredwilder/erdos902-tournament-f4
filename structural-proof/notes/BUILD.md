# f(4) >= 49 — PROOF OBJECT + RECEIPTS

Lean 4.31.0-rc1, Mathlib 919544d4309104b3f19724b0e6e48c701d27948f.
`F4Struct.lean` 3.07s, `F4Reject.lean` 1.03s, both exit 0 from a clean build.

## What is PROVED here (axioms: propext, Classical.choice, Quot.sound — no sorry, no native_decide)

`F4.no_S4_of_card_le_48` : a tournament with property S4 and 4 <= |V| <= 48 is impossible,
given one named external hypothesis `hExt`.

Chain, all proved in `F4Struct.lean`:
- `dominating_iff`      — a set has no dominator IFF it is a dominating set (the mechanism
                          behind Reid Prop 14; this is what turns "no x dominates S" into |S| >= gamma)
- `triple_ge_five`      — S4 => every triple has >= 5 common in-neighbours. Stated for an
                          ARBITRARY triple, which the (v,x,h) application requires.
- `pair_ge_eleven`      — => every pair has >= 11
- `vertex_ge_23`        — => every in-degree >= 23
- `exists_indeg_23_le`  — |V| <= 48 => some vertex has in-degree EXACTLY 23
- `core_regular`        — saturation 253 = 23*11 forces the core 11-regular
- `core_doubly`         — saturation 55 = 11*5 forces lambda = 5, i.e. DRT(23,11,5)
- `core_hasS3`          — the core has S3
- `mask_admissible`     — |W_x cap I_H(h)| <= 6 for every outside mask
- `bad_covered`         — every bad 4-set of the core lies in some outside mask
- `badCount_le_sum`     — hence badCount <= sum of repair counts
- `outside_card_eq`     — |O| + 24 = |V|

JOB 3 (isomorphism transport) — replaces sampled relabelings with theorems:
- `hasS3_transport`, `badSets_image`, `badSets_card_transport`,
  `maskOf_transport`, `admissible_transport`, `repairCount_transport`

JOB 2 (`F4Reject.lean`) — 35 kernel certificates that the other catalogue rows fail S3.
axioms = [propext] ONLY.

## STRONGER THAN THE DROP: order 47 dies too
The bound is `|V| <= 48`, not `= 48`. At 47 the same argument yields a vertex of in-degree 23
and |O| = 23, so 23*66 = 1518 < 2475. Reid & Brown's triply-regular theorem (used for
Corollary 7's step from 47 to 48) is NOT needed.

## THE ONE EXTERNAL HYPOTHESIS `hExt`
For a core that is a DRT(23,11,5) with S3: badCount >= 2475, and every admissible outside mask
repairs <= 66. Discharging it needs exactly two things, neither proved here:
 1. H2 — published completeness of the 37 DRT(23,11,5) classes (McKay/Spence). Cited, not reproved.
 2. The finite computation on rows 35/36 (f4_finite.lean, `native_decide`; Python-replicated:
    results.json regenerated byte-identically from a deleted file).
Transport from "isomorphic to a row" to "these numbers" is Job 3 above; wiring Job 3 to the
concrete Fin 23 catalogue is the remaining connective step.

## NEGATIVE CONTROLS — every load-bearing number verified to be load-bearing
| mutation | result |
|---|---|
| 2475 -> 1584 | build FAILS (omega) |
| capacity 66 -> 200 | build FAILS |
| \|V\| <= 48 -> <= 49 | build FAILS (omega) — the bound is exactly 48 |
| one S3 certificate corrupted | build FAILS (`decide` proves the statement FALSE) |

A green build that stays green under mutation proves nothing. These four say it is a gate.
